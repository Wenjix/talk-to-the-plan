{
  "buildCommand": "pnpm install --ignore-scripts && pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/((?!api|assets|vite\\.svg|pcm-processor\\.js).*)",
      "destination": "/index.html"
    }
  ]
}
